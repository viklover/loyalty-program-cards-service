server:
  port: 80

spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/loyalty-program?currentSchema=cards
    username: postgres
    password: postgres
  flyway:
    url: jdbc:postgresql://localhost:5432/loyalty-program?currentSchema=cards
    user: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
